<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-800">
    <div class="h-full w-full bg-zinc-800 p-10 text-white">
        <h1 class="mb-5 text-blue-500 text-xl"><a href="/">Go to Create Page </a></h1>
        <h1 class="mb-3 text-2xl" >All Users </h1>
            <div class="users flex gap-3 flex-wrap">
           <% if(users.length > 0) { %>
            <% users.forEach((user)=>{ %> 
                <div class="user max-w-72 p-4 bg-slate-800  rounded-lg">
                    <div class="w-full h-60  bg-cover overflow-hidden">
                        <img class="h-full w-full object-cover rounded-lg " src="<%=user.image %>" alt="">
                </div>
                    <h3 class=" mt-3 text-xl tracking-tighter "> <%=user.name %></h3>
                    <h5 class="text-zinc-500" > <%=  user.email %> </h5>
                    <div class="flex gap-3 ">
                        <a class="text-green-500" href="/edit/<%=user._id%>">Edit User</a>
                        <a class="text-red-500" href="/delete/<%=user._id%>">Delete User</a>
                    </div>
                </div>
           <% }) %>
           <% } else {  %>
            <h1 class="mb-5 text-zinc-500 mt-9 text-xl"> No users are there add some users</h1>
           <%  } %>
            </div>
    </div>
</body>
</html>