<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes Taking App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        
        <div class="form px-10 py-10 bg-zinc-900 text-white">
            <form action="/create" method="post">
                <input class="block px-5 bg-zinc-800 rounded-xl m-2 w-full" type="text" placeholder="title goes here" name="title" id="">
                <textarea class="block px-5 py-2 bg-zinc-800 m-2 resize-none w-full rounded-xl " name="details" id="" cols="30" rows="5" placeholder="write your title details"></textarea>
                <input class="bg-green-600 rounded-lg mx-2 m-2 w-[110px] h-8 " type="submit" value="create Task">
            </form>    
        </div>

        <div class="tasks flex gap-2 flex-wrap px-12">
            <% if(files.length > 0){ %>
            <% files.forEach((val)=>{ %>
                <div class="task min-w-72 px-3 py-2 rounded-lg bg-zinc-800 ">
                    <h1 class="text-white text-2xl tracking-tighter overflow-hidden whitespace-normal"><%=val%></h1>
                    <a class="text-blue-500 inline-block mt-2" href="/files/<%= val %>">Read More</a>
                </div>
            <% })%>
            <% } else { %>
                <h3 class="text-zinc-600 px-3">No Tasks Yet</h3>
            <% } %>

        </div>
    </div>

</body>
</html>